name: risingverse
channels:
  - conda-forge
  - defaults
dependencies:
  - click
  - emcee
  - gspread=3.1.0
  - numpy>=1.14
  - netCDF4
  - oauth2client
  - pandas=0.25  # Move to v1 if no deprecation warnings.
  - pip
  - python=3.7
  - pyyaml
  - pytest
  - scipy
  - statsmodels
  - xarray=0.9
  - pip:
    - impactlab-tools
    - metacsv
    - netCDF4==1.3
