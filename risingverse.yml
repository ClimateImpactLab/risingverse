name: risingverse
channels:
  - conda-forge
  - defaults
dependencies:
  - click
  - emcee
  - gspread=3.1.0
  - numpy>=1.14
  - netCDF4=1.3.1
  - oauth2client
  - pandas=0.25  # Move to v1 if no deprecation warnings.
  - pip
  - python=3.6
  - pyyaml
  - pytest
  - scipy
  - statsmodels
  - xarray=0.10
  - pip:
    - impactlab-tools
    - metacsv
